# 잘들어 — 받아쓰기 연습장 기획 구상안 (v1.2)

> 상태: **1차(MVP) 구현 완료.** Q1~Q13 결정 반영. 남은 것은 실물 인쇄 확인.
> 서비스명 **잘들어** / 배포 **https://badabada.vercel.app**
> (리포지토리 `github.com/kimhyunwoooo/bada-web`. 인프라명과 서비스명 분리는 흔한 구성이라 문제없음)

---

## 0. 확정 사항 (v0.1 답변)

| # | 결정 | 파급효과 |
|---|---|---|
| Q1 | 마침표 줄 첫머리 → **이전 줄에 매달기** | 파서에 금칙 처리 1줄 추가 |
| Q2 | **TTS 제외.** 저학년이라 부모가 옆에서 지도. 육성으로 불러줌 | ⚠️ **부모가 읽을 문장을 볼 화면이 필요해짐 → §3.4 신설** |
| Q3 | **Pretendard 단일 폰트.** 고딕이라 가독성 좋고 라이선스 무료 | 폰트 1종으로 통일 → 로딩 단순. ⚠️ 따라쓰기 흐린 글자 구현 방식 주의 → §5 |
| Q4 | **URL 공유 채택** | Q2와 결합해 §3.4의 전달 수단이 됨 → §8.1 |
| Q5 | **문항 수 1~10 가변** | ⚠️ 문항이 적으면 A4가 휑함 → 칸 크기 `auto` 모드 필요 |
| Q6 | 서비스명 **"잘들어"** | ⚠️ 제품 정체성이 "듣고 쓰기"에 있음 → 3종 중 주인공 재배치 필요 → §3.5 |
| Q7 | 인쇄물에 워터마크·출처 **없음** | QR·링크는 화면에만 |

---

## 1. 한 줄 정의

**부모가 받아쓰기 문장을 입력하면, 그 자리에서 인쇄 가능한 3종 학습지를 만들어 주는 웹앱.**

로그인·설치·결제 없이 "입력 → 만들기 → 인쇄"까지 30초. 이것이 유일한 핵심 가치이고,
나머지 모든 기능은 이 30초를 방해하지 않는 선에서만 붙인다.

---

## 2. 이 프로젝트의 진짜 난이도: 칸 나누기 규칙

UI보다 **문장을 칸으로 쪼개는 규칙**이 제품 품질을 결정한다. 첨부 이미지에서 역산한 규칙:

| 입력 | 칸 구성 | 슬롯 |
|---|---|---|
| `신나요.` | `신` `나` `요` `.` | 4칸 |
| `예쁘게 말해요.` | `예` `쁘` `게` **V** `말` `해` `요` `.` | 7칸 + V 1 |
| `곰이 그림책을 봅니다.` | `곰` `이` **V** `그` `림` `책` `을` **V** `봅` `니` `다` `.` | 10칸 + V 2 |

**확정 규칙 (R1~R6)**

- **R1.** 글자 1개 = 칸 1개. 한글 음절·숫자·영문자 모두 동일.
- **R2.** 문장부호(`.` `,` `!` `?` `"` `'`)도 **독립된 한 칸**을 차지한다.
- **R3.** 띄어쓰기는 **칸이 아니다.** 칸 사이에 빨간 `V` 마커로만 표시한다.
- **R4.** 줄바꿈은 **어절 단위**로만 일어난다. 한 어절이 두 줄로 쪼개지지 않는다.
- **R5.** 한 줄 최대 슬롯 수는 용지 폭에서 역산(A4 기준 12~14칸). 초과 시 다음 줄로 흐른다.
- **R6.** ✅Q1 닫는 문장부호(`.` `,` `!` `?` 닫는 따옴표)가 줄 첫머리에 오면 **이전 줄 끝에 매단다.**
  구현: 어절 그룹핑 시 부호를 앞 어절에 붙여두면 R4가 자동으로 처리한다.

**입력 정규화**
- 앞뒤 공백 trim, 연속 공백은 1개로 축약
- 여는 따옴표로 시작하는 문장 `"안녕?"` → 여는 따옴표도 한 칸

파서는 `parseSentence()` **순수 함수 하나**로 격리하고, 여기에만 단위 테스트를 붙인다.
UI는 테스트하지 않는다. 투자 대비 효과가 여기 집중돼 있다.

```
parseSentence("곰이 그림책을 봅니다.")
→ [
    { type: 'word',  cells: ['곰','이'] },
    { type: 'space' },
    { type: 'word',  cells: ['그','림','책','을'] },
    { type: 'space' },
    { type: 'word',  cells: ['봅','니','다','.'] },
  ]
```

---

## 3. 학습지 3종 + 부모 화면 1종

| # | 이름 | 구성 | 난이도 | 대상 |
|---|---|---|---|---|
| 1 | **보고 쓰기** | 위에 작은 답안 칸, 아래에 큰 빈칸 | 쉬움 | 종이 |
| 2 | **따라 쓰기** | 빈칸 안에 옅은 회색 글자 | 중간 | 종이 |
| 3 | **듣고 쓰기** | 빈칸만 + 상단 안내문 | 어려움 | 종이 |
| — | **불러주기** | 문장 1개를 화면 가득 | — | **화면 (부모용)** |

### 3.4 불러주기 화면 — Q2 결정이 만든 빈틈을 메우는 화면 ⭐신설

TTS는 뺐지만 **"부모가 읽을 문장을 보여주는 화면"은 여전히 필요하다.**
듣고 쓰기 학습지에는 정의상 답이 없기 때문에, 부모는 지금 기획대로면
학습지를 한 장 더 뽑거나 입력 화면으로 되돌아가야 한다.

> TTS 제안에서 무거웠던 건 **음성 부분**이지 화면이 아니었다.
> 음성 합성·속도 조절·자동 대기 로직을 전부 걷어내면, 남는 건 화면 하나다.

실제 경로: `/read/:id` (QR·공유 링크는 `/read?s=...`)

```
        ┌─────────────────────────┐
        │                     3/8 │
        │                         │
        │   곰이 그림책을 봅니다.    │   ← 화면 가득, 40px+
        │                         │
        │   ◀ 이전      다음 ▶     │   ← 큰 터치 영역
        └─────────────────────────┘
```

- 문장 하나를 화면 가득. 부모가 폰을 들고 소리 내어 읽는다
- 좌우 넘기기(스와이프 + 큰 버튼), 상단에 `3/8` 진행 표시
- 전체 목록 보기 토글 — 급수표 훑어보기용
- **코드 비용:** 배열 인덱스 + 좌우 버튼. TTS 대비 훨씬 가볍다

**Q4(URL 공유)와 결합하면 플로우가 매끄러워진다:**

```
  듣고쓰기 인쇄 ──▶ 종이는 아이에게
        │
        └─▶ 화면의 QR/링크 ──▶ 부모 폰에서 불러주기 화면 열기
                                (다른 방에서도, 설거지하면서도)
```

### 3.5 "잘들어" 네이밍이 요구하는 것 ⭐

서비스명이 **"잘들어"** 인 이상, 제품의 주인공은 **듣고 쓰기**다.
그런데 현재 기획은 난이도 오름차순(보고 → 따라 → 듣고)이라 듣고 쓰기가 맨 끝에 있다.

- **인트로 히어로**: 학습지 견본을 실물 종이처럼 놓아 보여 준다
- **결과 화면 카드 순서**: 듣고 쓰기를 맨 앞에 세운다
- **불러주기 화면**은 부가 기능이 아니라 **이름값을 하는 화면**으로 취급한다

**톤 규칙 (카피 작성 시 반드시 지킬 것)**

| 대상 | 말 거는 상대 | 예시 |
|---|---|---|
| 브랜드명 | 아이 | "잘들어" |
| UI 전체 | **부모** | "문장을 입력하세요", "3장 만들어졌어요" |
| 인쇄물 | **아이** | "잘 듣고 또박또박 써 보세요" |

UI에서 아이 말투를 쓰면 안 된다. 실제 조작자는 부모다.

---

## 4. 화면 흐름

```
  ┌──────────┐  직접 만들기  ┌──────────┐   만들기   ┌──────────┐
  │  인트로   │ ───────────▶ │   입력    │ ────────▶ │   결과    │
  │    /     │              │ /create  │           │ /result  │
  └──────────┘              └──────────┘           └────┬─────┘
       ▲                                                │
       │ 최근 세트                            ┌─────────┴─────────┐
       │                                    ▼                   ▼
       │                          ┌──────────────┐    ┌──────────────┐
       └──────────────────────────│ 인쇄 미리보기  │    │  불러주기 화면 │
                                  │/sheet/:id/   │    │  /read/:id   │
                                  │   :type      │    │              │
                                  └──────┬───────┘    └──────────────┘
                                         ▼                  (부모용·화면)
                                  window.print()
```

### 4.1 인트로 `/`
- 히어로: `hero-display` 56px + 한 줄 카피 + `button-primary` "직접 만들기"
- 하단: **최근 만든 세트**(localStorage, 있을 때만)

⚠️ **v1.1에서 덜어냄.** 원래는 3종 타입을 소개하는 알터네이팅 타일과 샘플 세트 영역을 두었으나,
30초 안에 만들러 온 사람에게 설명 타일은 통과 의례일 뿐이었다. 히어로 리드 문구가 이미
세 종류를 말하고 있으므로 타일은 중복이다. 인트로는 **"만들기"와 "다시 꺼내기"** 두 가지만 한다.

### 4.2 입력 `/create`

**① 붙여넣기 우선 ⭐신규 제안**

실제 사용 시나리오를 따라가 보면, 부모는 문장을 **직접 창작하지 않는다.**
학교에서 알림장·가정통신문·학급 카톡으로 **받아쓰기 급수표를 이미 받았다.**
그걸 그대로 옮겨 적는 게 이 화면의 실제 작업이다.

→ **여러 줄 붙여넣기 입력**을 1순위 진입점으로 둔다.

```
  ┌─────────────────────────────────┐
  │ 받아쓰기 문장을 붙여넣으세요       │
  │ ─────────────────────────────── │
  │ 1. 신나요.                      │  ← 카톡에서 복사한 그대로
  │ 2. 뿌듯해요.                    │
  │ 3. 동실동실                     │
  └─────────────────────────────────┘
           ↓ 자동 분리
  줄바꿈으로 나누고 앞머리 번호(`1.` `1)` `①`)를 제거해 10칸에 배분
```

**두 모드는 같은 목록을 보는 두 가지 방식이다 (v1.2).**

⚠️ v1.1에서 이걸 "서로 다른 입력 수단" 두 개로 만든 것이 설계 실수였다.
모드를 바꿀 때마다 한쪽을 비워버려서, 오타 하나 고치러 붙여넣기로 돌아가면
붙여넣은 내용이 통째로 날아갔다. 오갈 수 없는 전환은 전환이 아니다.

올바른 모델은 **하나의 문장 목록 + 두 가지 표현**이다.
붙여넣기는 목록을 텍스트로, 한 문장씩은 같은 목록을 행으로 보여 줄 뿐이다.

- 붙여넣기로 전환 → 지금 문장들이 한 줄에 하나씩 textarea에 채워진다
- 한 문장씩으로 전환 → textarea를 파싱해 행으로 펼친다 (앞머리 번호 제거)
- **어느 쪽에서든 바로 `학습지 만들기`** — 붙여넣고 한 번에 끝낼 수 있어야 한다
- 새로 만들 때 기본값 = 붙여넣기 / 고치러 들어왔을 때 = 한 문장씩

"덮어씁니다" 경고는 사라졌다. 덮어쓰는 게 아니라 같은 데이터를 다르게 보는 것이므로
경고할 일 자체가 없다. **경고가 필요하다는 건 대개 모델이 틀렸다는 신호다.**

**② 개별 입력**
- 처음엔 3칸만 보이고 채우면 다음 칸이 생긴다 (빈 칸 10개는 압박감이 크다)
- **각 입력창 아래 칸 배치 실시간 미리보기** — 이 화면의 킬러 요소.
  "13칸이라 한 줄에 안 들어갑니다" 같은 피드백을 입력 중에 준다
- 검증: 빈 문장 제거 / 길이 초과 경고 / 최소 1문장 / 문장 끝 마침표 누락 힌트

### 4.3 결과 `/result`
- 학습지 3장 카드(축소 썸네일 + `열기`) + **불러주기 화면 카드**
- `문장 수정` 버튼으로 `/create` 복귀 (내용 유지)
- `링크 복사` / `QR 보기`

### 4.4 인쇄 미리보기 `/sheet/:id/:type`
- **화면에 A4 실물 비율 그대로** 렌더 (흰 종이가 회색 배경 위에 떠 있는 형태)
- 상단 고정 바(인쇄 시 숨김): `타입 전환` `칸 크기` `V 표시` `머리글` `인쇄`
- `window.print()` → 브라우저 인쇄 대화상자 (PDF 저장도 여기서 해결)

---

## 5. 인쇄 설계 (가장 자주 깨지는 부분)

- `@page { size: A4 portrait; margin: 0 }` + 종이 여백은 `.paper`의 `padding: 12mm`
  (⚠️ 구현하며 정정: `@page` margin과 padding을 함께 주면 여백이 두 번 들어간다.
  margin을 0으로 두어야 화면 미리보기와 인쇄 여백이 정확히 같아진다)
- **모든 치수를 `mm` 로 고정.** px/rem을 쓰면 브라우저·배율마다 결과가 달라진다
- 칸 크기: `크게 14mm` / `보통 12mm` / `작게 10mm` / **`자동`(기본, 8~15mm)**
  ✅Q5 문항 수 가변이므로 `auto`가 필요하다 — 4문항이면 大, 10문항이면 中을 자동 선택해
  A4 한 장을 자연스럽게 채운다. 수동 3단은 그대로 남긴다
- 페이지 분할은 **JS가 확정한다** (⚠️ 구현하며 정정). CSS `break-inside: avoid`에 맡기면
  화면 미리보기와 인쇄 결과가 어긋난다 — 안전망으로만 남겨 둔다
- `print-color-adjust: exact` — 옅은 회색 글자와 빨간 V가 인쇄에서 날아가지 않게
- 머리글: `이름 ______  날짜 ____  점수 ___` 한 줄 (on/off)
- 검증: Chrome / Edge / Safari 실제 인쇄 확인. **1차 완료 기준에 포함**

### ⚠️ 따라쓰기 흐린 글자 — `opacity` 금지

✅Q3 Pretendard 단일 폰트로 가면, 따라쓰기의 "옅은 글자"는 **색상값으로 직접 지정**해야 한다.

```css
/* ❌ 브라우저·프린터 드라이버마다 결과가 다르고, 인쇄에서 아예 사라지기도 한다 */
.trace-cell { opacity: .25 }

/* ✅ 회색 자체를 색으로 지정 + 폰트 웨이트를 낮춘다 */
.trace-cell { color: #c8c8c8; font-weight: 300 }
```

Pretendard는 Light(300)가 있으므로 웨이트를 낮추면 획이 얇아져 따라 쓰기 더 좋다.
정확한 회색값은 **실제 인쇄 테스트로 확정**한다(모니터에서 적당해 보이는 값은 대개 인쇄에서 너무 흐리다).

---

## 6. 기술 스택

| 영역 | 선택 | 근거 |
|---|---|---|
| 프레임워크 | **Vue 3 `<script setup>` + TypeScript** | 요청 스택. 칸 데이터 구조에 타입이 큰 도움 |
| 빌드 | **Vite** | 표준 |
| 라우팅 | **vue-router** (history) | 5개 라우트 |
| 상태 | **Pinia** 단일 스토어 `useSheetStore` | 2차 로그인·목록 확장 고려 |
| 스타일 | **순수 CSS + CSS 변수 토큰** | DESIGN-mastercard.md가 이미 완성된 토큰 세트. Tailwind는 오히려 번역 손실 |
| 아이콘 | **자체 인라인 SVG 세트** (`Icon.vue`) | 24 그리드·1.5 굵기로 통일. 외부 라이브러리 없이 선 굵기를 시스템에 맞춘다 |
| 저장 | **localStorage** `bada.sheets.v1` | 1차엔 DB 불필요 |
| URL 압축 | **lz-string** (`compressToEncodedURIComponent`) | 1.5KB, 검증된 라이브러리 |
| 테스트 | **Vitest** — 칸 파서만 | 나머지는 수동 확인 |
| 배포 | **Vercel** (`badabada.vercel.app`) | 서버 없음 |

**폰트 — ✅Q3 Pretendard 단일**
- UI·인쇄·칸 글자 전부 Pretendard. 폰트 1종이면 로딩·FOUT 관리가 단순해진다
- ⚠️ 한때 명조(Cormorant Garamond)를 얹어 봤으나 되돌렸다. 라틴 전용 서체라
  한글이 시스템 기본 폰트로 떨어지고, 키즈 에듀의 인상과도 맞지 않았다
- 디자인 컨셉은 Apple → LuxCart → Starbucks → **Mastercard**로 옮겨 왔다.
  토큰은 `tokens.css` 한 파일에 모여 있고 종이는 그 밖에 있으므로,
  컨셉이 또 바뀌어도 학습지 출력은 흔들리지 않는다
- ⚠️ **전체 한글 웹폰트는 무겁다(수 MB).** `Pretendard Dynamic Subset` CDN을 쓰면
  실제 사용 글자만 내려받는다. 인쇄물 품질에 직결되므로 폰트 로딩 완료 전 인쇄를 막을 것
  (`document.fonts.ready` 확인 후 `window.print()`)
- 웨이트: 300 / 400 / 600 / 700만 사용 (Apple 토큰의 "500 금지" 규칙 그대로)

**디자인 토큰 적용 시 유의 (DESIGN-mastercard.md)**
- 페이지 바탕은 순백이 아니라 **따뜻한 퍼티 크림 `#F3F0EE`**. 이 온도가 시스템의 출발점이다
- 본문 글자는 살짝 따뜻한 near-black `#141413`. 순수 검정을 쓰지 않는다
- **본문 굵기 450**이 이 시스템의 서명이다. 400으로 내리면 정체성이 납작해진다.
  Pretendard Variable이 450을 지원해 가능하다
- 제목 자간은 **-2%**로 단단히 묶는다. 넓은 자간은 14px 아이브로우에만 쓴다
- **아이브로우 앞의 작은 점**을 빼지 않는다 — 이 시스템의 표식이다
- 반경은 세 가지뿐이다: 작거나(≤6), 크거나(20~40), 완전한 알약(999). **8~12px 중간값은 쓰지 않는다**
- ⚠️ **키즈 조정 두 가지**: 버튼을 원본의 20px 대신 **풀 필**로, 카드를 20 대신 **40px**로 올렸다.
  둥글수록 부드럽게 읽히기 때문이다. 나머지는 원본 그대로
- **Signal Orange `#CF4500`는 쓰지 않는다.** 원본에서 동의·법적 고지 전용이고,
  마케팅 CTA에 쓰면 그 신호가 희석된다. 실제 강조는 Light Signal Orange `#F37338`가 맡는다
- 그림자는 방향을 가진 빛이 아니라 대기의 쿠션이다. **48px 이상 퍼지고 10% 이하**로만
- **인쇄물은 디자인 토큰의 지배를 받지 않는다.** 종이는 흰 바탕 + 흑백 + 빨강 V 뿐.
  크림색을 인쇄하면 잉크만 먹고 지저분해진다

---

## 7. 데이터 모델

```ts
type SheetType = 'see' | 'trace' | 'listen'   // 보고 / 따라 / 듣고

interface Sentence {
  id: string
  text: string          // 원문. 칸 분해는 렌더 시점에 파생
}

interface SheetSet {
  id: string            // nanoid
  version: 1
  title: string
  sentences: Sentence[] // 1~10
  createdAt: number
  updatedAt: number
  options: {
    cellSize: 'auto' | 'sm' | 'md' | 'lg'
    showSpaceMark: boolean   // V 표시
    showHeader: boolean      // 이름/날짜/점수 줄
  }
}
```

**`version` 필드를 처음부터 넣는다.** 2차에서 서버로 옮길 때 마이그레이션 비용이 사라진다.

---

## 8. 채택 기능 상세

### 8.1 URL 공유 ✅Q4

- `SheetSet`을 JSON → `lz-string`으로 압축 → `?s=...` 쿼리에 담는다
- 한글 문장 10개면 압축 후 대략 300~500자. 카톡 전송에 무리 없는 길이
- 링크를 열면 그 자리에서 세트가 복원되고 localStorage에 저장된다 (로그인 불필요)
- **`옵션`도 함께 담는다** — 받는 쪽에서 칸 크기가 달라지면 안 된다

**QR 코드 (제안)**
- 인쇄 미리보기·결과 화면에 QR 표시 → 부모 폰으로 **불러주기 화면** 바로 열기
- ✅Q7에 따라 **종이에는 넣지 않는다.** 화면에만
- §3.4의 "다른 방에서도 불러주기" 플로우를 완성하는 조각

### 8.2 세트 복제 ⭐신규 제안

받아쓰기는 **매주 반복되는 일**이다. 지난주 세트를 복제해 문장 몇 개만 바꾸는 게
매번 10문장을 새로 입력하는 것보다 실제 사용 패턴에 가깝다.
localStorage만으로 구현되고 코드도 몇 줄이다. → 1차 후보

### 8.3 그 외 채택
- **문항 순서 섞기** — 아이가 순서를 외워버리는 걸 방지

### 8.4 보류 (2차 이후)
- **추천 문장 (구 "샘플 세트")** — v1.1에서 1차 범위에서 뺐다.
  급조한 예시 3종으로는 값어치가 없고, 오히려 인트로만 길어졌다.
  2차에서 **1급~10급 실제 급수표 데이터를 모아** 무작위로 섞어 추천하는 형태로 다시 만든다.
  그때는 "빈 화면 메우기"가 아니라 그 자체로 쓸 만한 콘텐츠가 된다.
- 낱말 모드 (문장 대신 단어 목록 — 칸 배치 규칙이 달라짐)
- 오답 노트 — 틀린 문장만 모아 새 세트 (2차 로그인과 함께)
- 태블릿 직접 필기 (Canvas) — 별개 제품에 가깝다
- 자동 채점 — 필기 인식 필요. 범위 밖
- **TTS** — ✅Q2로 제외. 다만 §3.4 화면이 있으면 나중에 🔊 버튼 하나만 얹으면 되도록
  데이터 구조는 열어둔다 (설계 부채 없음)

### 8.5 반대
- 학년별 커리큘럼 DB 내장 → 콘텐츠 관리 비용이 앱 자체보다 커진다
- 회원가입 강제 → 30초 가치 제안을 정면으로 파괴. **2차에서도 로그인은 선택**

---

## 9. 범위 구분

### 1차 (MVP)
- [x] 칸 파서 + 단위 테스트 (R1~R6)
- [x] 인트로 / 입력 / 결과 / 인쇄 미리보기 / **불러주기** 5개 화면
- [x] 학습지 3종 렌더링
- [x] A4 인쇄 CSS + `auto` 칸 크기 + 페이지 자동 분할
- [x] 입력 방식 선택 (붙여넣기 / 한 문장씩)
- [x] localStorage 저장 + 최근 세트 + 세트 복제
- [x] URL 공유 + QR
- [x] 칸 크기 / V 표시 / 머리글 옵션
- [x] 반응형 (모바일 입력·불러주기 → 데스크톱 인쇄)
- [x] **배포** — https://badabada.vercel.app (SPA 리라이트 실서버 확인 완료)
- [ ] **실물 프린터 출력 확인** — 칸 크기와 따라 쓰기 회색값 최종 조정
- [ ] **Safari 인쇄 확인** (Chromium만 검증됨)

### 2차
- 로그인 (소셜 1종. Supabase Auth 또는 Firebase — 서버 코드 없이 가능)
- 세트 목록 / 수정 / 삭제 / 폴더
- 서버 저장 + 기존 localStorage 데이터 마이그레이션
- 학습 기록 (푼 날짜, 점수 직접 입력)
- 오답 노트

**2차 사전 대비:** 1차에서 `SheetStore`의 CRUD 인터페이스를 async 로 정의해두면
(내부는 localStorage 동기 처리) 2차에서 저장소만 갈아끼우면 된다.

---

## 10. 2라운드 결정 (모두 반영 완료)

| # | 질문 | 결정 |
|---|---|---|
| **Q8** | **불러주기 화면**(§3.4)을 1차에 넣을까? | **넣자.** TTS와 달리 화면 하나뿐이고, Q2로 생긴 빈틈을 정확히 메운다. 서비스명 "잘들어"의 근거이기도 하다 |
| **Q9** | **붙여넣기 일괄 입력**(§4.2)을 1차에 넣을까? | **넣자.** 부모는 문장을 창작하지 않고 급수표를 옮겨 적는다. 30초 목표에 가장 크게 기여 |
| **Q10** | 세트 제목 기본값 — 날짜? **급수**? | 학교는 "받아쓰기 3급" 식으로 부른다. `3급`을 앞에, 날짜를 뒤에 두는 게 실제 호칭에 가깝다 |
| **Q11** | 문항이 A4 한 장을 넘치면? | 2페이지 자동 분할 + 미리보기에 "2장이 인쇄됩니다" 안내 |
| **Q12** | 문장 길이 상한? | **2줄(약 24~28칸)까지 허용, 초과 시 경고만.** 차단하면 부모가 답답해한다 |
| **Q13** | 인트로 히어로에 무엇을 보여줄까? | 실제 칸 미리보기(글자 없는 빈 칸 격자). 사진 자산이 없고, 격자 자체가 이 제품의 얼굴이다 |

---

## 11. 구현 메모

구현하면서 확정하거나 바로잡은 것들:

- **칸을 무한정 키우지 않는다.** 문항이 적을 때 칸만 키우면 저학년 손 크기에 비해 과해지고
  한 줄에 들어가는 칸 수가 줄어 문장이 불필요하게 두 줄이 된다.
  칸은 15mm에서 멈추고, 남는 세로 공간은 **문항 사이 여백**으로 흘려보낸다 (상한 16mm).
- **여백 분배는 페이지마다 따로 계산한다.** 첫 장 기준으로 계산한 간격을 다른 장에 그대로 쓰면
  그 장이 넘친다. 페이지를 채우는 값은 반올림이 아니라 항상 내림 — 반올림은 0.01mm 초과를 만든다.
- **줄바꿈과 페이지 분할을 CSS가 아니라 JS로 확정했다.** flex-wrap과 `break-inside`에 맡기면
  화면 미리보기와 인쇄 결과가 어긋난다. `break-inside: avoid`는 안전망으로만 남겼다.
- **`@page`의 margin은 0**, 종이 여백은 `.paper`의 padding이 담당한다.
  둘 다 주면 여백이 두 번 들어간다.
- **R6은 정규화 단계에서 해결된다.** 부호 앞의 공백을 없애 부호를 앞 어절에 붙이면,
  어절 단위 줄바꿈(R4)이 자동으로 R6을 만족시킨다. 별도 금칙 로직이 필요 없다.

### 검증 상태

- 단위 테스트 54개 통과 (칸 파서 / 레이아웃 / 붙여넣기 / 공유 링크)
- Chromium 인쇄 파이프라인에서 7개 조합 확인 — 종이 210×297mm 정확,
  2장짜리도 두 번째 장이 정확히 y=297mm에서 시작, 문항 넘침 0건
- **미확인:** 실물 프린터 출력, Safari 인쇄

### 다음 단계

1. 실제로 인쇄해서 칸 크기(현재 auto 11.5~15mm)와 따라 쓰기 회색값(`--mark-trace: #c8c8c8`) 확정
2. Safari 인쇄 확인
3. ~~배포~~ ✅ **완료** — https://badabada.vercel.app

   ⚠️ 짧은 이름(`bada`, `bada-web`)은 이미 남이 선점하고 있었다. `*.vercel.app`은
   전 세계 공용이라 흔한 단어는 대부분 비어 있지 않다.
   또한 **프로젝트 이름을 바꾸면 기존 자동 URL이 즉시 끊긴다** — 이미 공유한 링크가 있다면
   이름 대신 Domains에 새 주소를 추가하는 편이 안전하다.
   도메인을 막 추가한 직후에는 TLS 인증서 발급 전까지 몇 분간 연결이 걸릴 수 있다.
4. 2차 스펙 논의
